# ECU SOP 实现确认

> 本文记录 ECU 侧实际采用（或计划采用）的 SOP 接收、限幅、扭矩换算和 `0x4A4` 发送方案。
> **状态：待 ECU 负责人填写。** 未填写完成前，[`CANB接口.md`](CANB接口.md) 中所有 ECU 相关规则、周期和策略都只视为暂定方案，不代表 ECU 最终实现。
> ECU 负责人应结合实际模型、整车控制需求、CANB 容量和误触发风险给出最终方案；如果当前 DBC 或本仓库文档不合理，可以提出修改请求，而不是照抄现有内容。
> 填写完成后，应把本文结论同步到 `Vehicle_CanB.dbc`、[`CANB接口.md`](CANB接口.md) 和 `CHANGELOG.md`，并删除对应待确认项。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 实现负责人 |  |
| 最后确认日期 |  |
| Simulink / MATLAB 版本 |  |
| 模型仓库或路径 |  |
| CAN 模块/工具 |  |
| 当前阶段 | [ ] 尚未实现 [ ] 已实现 E2E 仿真 [ ] 已接入实车 [ ] 已完成联调 [ ] 已确认 |

## 2. 协议布局确认

请核对 DBC 文件中每个 ID 的布局是否与 ECU 模型一致。**如果一致，勾选“是”；如果不一致，填写期望值并说明理由。**

| ID | DLC | 当前布局 | ECU 模型实际 | 是否一致 | 需要修改的内容 |
| --- | --- | --- | --- | --- | --- |
| `0x4A0` | 8 | 见 DBC |  | [ ] 是 / [ ] 否 |  |
| `0x4A3` | 8 | 见 DBC |  | [ ] 是 / [ ] 否 |  |
| `0x4A4` | 8 | 见 DBC |  | [ ] 是 / [ ] 否 |  |

## 3. ECU 接收 `0x4A0 / 0x4A3`

### 3.1 报文配置确认

| 项目 | ECU 填写 |
| --- | --- |
| 标准 ID | `0x4A0` / `0x4A3` |
| DLC | 8 |
| 位率 | 500 kbit/s |
| 帧类型 | 经典 CAN 数据帧 |
| 解析方式 | [ ] CAN Pack/Unpack [ ] 自写 C [ ] 其他： |

### 3.2 `0x4A0` 字段确认

| 字段 | 位序 | 单位/比例 | ECU 最终确认值 |
| --- | --- | --- | --- |
| `DischargeCurrentLimit` | Byte0..1 | 小端，0.1 A/bit |  |
| `ChargeCurrentLimit` | Byte2..3 | 小端，0.1 A/bit |  |
| `DischargePowerLimit` | Byte4..5 | 小端，0.1 kW/bit |  |
| `ChargePowerLimit` | Byte6..7 | 小端，0.1 kW/bit |  |

### 3.3 `0x4A3` 字段确认

| 字段 | 位序 | ECU 最终用途 |
| --- | --- | --- |
| `SOPProtocolVersion` | Byte0 高 4 位 |  |
| `SOPSequence` | Byte0 低 4 位 |  |
| `LimitsValid` | Byte1 bit0 |  |
| `DriveAllowed` | Byte1 bit1 |  |
| `RegenAllowed` | Byte1 bit2 |  |
| `OvercurrentLimitZero` | Byte1 bit3 |  |
| `BMSFaultHold` | Byte1 bit4 |  |
| `SOPAckRequired` | Byte1 bit5 |  |
| `LimitsReduced` | Byte1 bit6 |  |
| `BatteryState` | Byte2 |  |
| `LimitReason` | Byte3..4 |  |
| `InputHealth` | Byte5 |  |
| `InterventionLevel` | Byte6 bit0..1 |  |
| `DischargeOvercurrentZero` | Byte6 bit2 |  |
| `ChargeOvercurrentZero` | Byte6 bit3 |  |
| `AckMissing` | Byte6 bit4 |  |
| `AckFresh` | Byte6 bit5 |  |
| `CurrentBelowClearThreshold` | Byte6 bit6 |  |
| `SOPCRC8` | Byte7 |  |

### 3.4 成组校验规则（ECU 最终方案）

请逐项填写实际行为：

| 场景 | ECU 实际行为 |
| --- | --- |
| 只收到 `0x4A0` |  |
| 只收到 `0x4A3` |  |
| CRC 错误 |  |
| 协议版本不是 1 |  |
| 会话计数与上一组重复 |  |
| 会话计数跳号 |  |
| 两次接收之间超过设定超时 |  |
| `LimitsValid=0` |  |
| 同时收到多个 ID 或来自多个节点 |  |

## 4. 当前暂定策略的合理性审查

以下是 BMS 侧目前写在文档里的“暂定策略”，不是最终要求。请逐项判断是否会造成误触发、动力突然归零或恢复过慢，并给出 ECU 实际采用值。**如果认为某一项过多限制，可以建议放宽，但必须说明理由和上限。**

| 项 | 当前暂定值 | ECU 实际值 | 是否会造成误触发/过严 | 理由 |
| --- | --- | --- | --- | --- |
| `0x4A0+0x4A3` 发送周期 | 10 ms |  | [ ] 是 / [ ] 否 |  |
| BMS 未收到有效组时的 ECU 动作 | 暂定立即零扭矩/300 ms 超时 |  | [ ] 是 / [ ] 否 |  |
| `LimitsValid=0` 时 | 暂定立即归零 |  | [ ] 是 / [ ] 否 |  |
| 限值为 0 时（仅一个方向） | 暂定该方向归零 |  | [ ] 是 / [ ] 否 |  |
| 放电过流时 | 暂定立即归零 |  | [ ] 是 / [ ] 否 |  |
| 回充过流时 | 暂定立即归零 |  | [ ] 是 / [ ] 否 |  |
| CRC 错误时 | 暂定不采用 |  | [ ] 是 / [ ] 否 |  |
| 单次丢帧 | 暂定等待下一组，不归零 |  | [ ] 是 / [ ] 否 |  |
| 报文恢复 | 暂定连续 2 组有效 |  | [ ] 是 / [ ] 否 |  |
| 扭矩恢复速度 | 暂定按原有速度 |  | [ ] 是 / [ ] 否 |  |

### 4.1 最终限值来源

请填写 ECU 最终采用的限制链：

```text
P_dis_final = min(?, ?, ?, ?)
P_chg_final = min(?, ?, ?, ?)
```

| 候选来源 | 是否使用 | 说明 |
| --- | --- | --- |
| BMS 放电电流上限 |  |  |
| BMS 放电功率上限 |  |  |
| BMS 回充电流上限 |  |  |
| BMS 回充功率上限 |  |  |
| 车辆模式功率上限 |  |  |
| ECU 本地/策略上限 |  |  |
| AMK/逆变器本地能力上限 |  |  |
| 踏板/制动请求限制 |  |  |

### 4.2 电流/功率到扭矩的换算

| 项目 | ECU 填写 |
| --- | --- |
| 单位/比例 |  |
| 四轮是否等比例 |  |
| 轮间分配是否与 BMS 有关 |  |
| 扭矩斜坡/平滑时间 |  |
| 驱动/回收方向定义 |  |

## 5. `0x4A4` 确认帧

### 5.1 最终发送方案

| 项目 | ECU 填写 |
| --- | --- |
| 发送周期（或触发条件） |  |
| Byte0 高 4 位版本 |  |
| Byte0 低 4 位采用的计数 |  |
| Byte1 各标志位的置位条件 |  |
| Byte2..3 最终放电功率 |  |
| Byte4..5 最终回充功率 |  |
| Byte6 主要限制来源 |  |
| Byte7 CRC 计算方式 |  |

### 5.2 主要限制来源枚举

当前候选值（BMS 侧暂定，最终以 ECU 实际输出为准）：

| 值 | 含义 |
| ---: | --- |
| 0 | 当前请求低于全部限制 |
| 1 | BMS 放电电流限制 |
| 2 | BMS 放电功率限制 |
| 3 | BMS 回充电流限制 |
| 4 | BMS 回充功率限制 |
| 5 | ECU 车辆模式功率限制 |
| 6 | ECU 或逆变器本地能力限制 |
| 7 | BMS SOP 无效或超时 |
| 8 | AMK 或动力系统故障 |
| 9 | 踏板、制动或扭矩请求检查 |
| 10 | 其他 ECU 本地限制 |
| 255 | 未知 |

ECU 最终使用哪些值，以及是否需要新增枚举，请在此确认。

## 6. 状态机和异常场景

### 6.1 上电/模式切换

| 场景 | ECU 最终行为 |
| --- | --- |
| ECU 上电但未收到 SOP |  |
| 进入充电模式 |  |
| 从充电切回驾驶模式 |  |
| 高压断开 |  |
| BMS 状态非高压接通 |  |

### 6.2 故障/降额场景

| 场景 | ECU 动作 | 响应时间 | 恢复条件 |
| --- | --- | --- | --- |
| 未收到有效 SOP 报文组 |  |  |  |
| `LimitsValid=0` 且限值为 0 |  |  |  |
| 放电过流归零 |  |  |  |
| 回充过流归零 |  |  |  |
| BMS 故障保持 |  |  |  |
| AMK/逆变器严重故障 |  |  |  |
| CANB 高负载丢帧 |  |  |  |
| ECU 自身故障 |  |  |  |

## 7. ID 优先级和发送频率

**说明：** 这里的“优先级”指 CAN 帧在 ECU/接收端软件中的处理优先级或调度顺序，不是 CAN 仲裁 ID 大小。发送频率指各节点实际发送周期。

| 报文 | 当前暂定周期 | 当前暂定优先级 | ECU/节点最终周期 | ECU/节点最终优先级 | 若调整需同步哪些文档 |
| --- | --- | --- | --- | --- | --- |
| `0x305` | 暂定 50 ms | 暂定高 |  |  |  |
| `0x502..0x509` | 暂定 10 ms | 暂定高 |  |  |  |
| `0x4A0/0x4A3` | 暂定 10 ms | 暂定高 |  |  |  |
| `0x4A4` | 暂定由 ECU 自定 | 暂定高 |  |  |  |

请按节点实际控制需求填写。如果某报文只是调试/HMI 显示或遥测用，不需要 10 ms 或不需要高优先级，请直接写实际值和理由；修改 ID、DLC、周期、优先级会影响 BMS/HMI/遥测，必须同步更新 DBC、接口文档、接收方代码和遥测对照。

## 8. 需要 ECU 同学补充的文档

**ECU 侧除了填写本文件，还需要为以下报文各提供一份简要说明，明确字段定义、单位、比例、有效范围、周期、发送节点/接收节点和异常处理。**

| 报文 | 必须说明 |
| --- | --- |
| `0x305` | 方向盘转角、油门开度、油压的字段、单位、比例、有效范围和周期 |
| `0x502..0x509` | 四轮映射、字段、单位、比例、诊断码、状态枚举和周期 |
| `0x4A0/0x4A3` | 由 BMS 提供；ECU 需确认解析和接收规则 |
| `0x4A4` | ECU 负责：发送周期、触发、字段、CRC、状态枚举 |

这些文档内容应整理到本仓库（例如 `docs/ECU报文说明.md`），或由 ECU 负责人在 PR 中提供可链接文档。只给 DBC、只给出信号名、或只写“能用”都不算完成。

## 9. 验证和联调结果

| 验证项 | 通过条件 | 实测结果 |
| --- | --- | --- |
| 正常收到 `0x4A0+0x4A3` | `0x4A4` 回显计数和状态正确 |  |
| 只停止 `0x4A0` | 按 ECU 最终策略退扭 |  |
| 只停止 `0x4A3` | 按 ECU 最终策略退扭 |  |
| CRC 错误 | 不采用、不刷新确认 |  |
| `LimitsValid=0` | 按 ECU 最终策略动作 |  |
| 放电过流 | 按 ECU 最终策略动作 |  |
| 回充过流 | 按 ECU 最终策略动作 |  |
| 恢复时序 | 按 ECU 实际恢复策略 |  |
| CANB 满载 | 无连续误超时、无意外扭矩跳变 |  |
| 实车/台架 | ECU、BMS、HMI、遥测四方结果一致 |  |

## 10. 确认结论

【ECU 负责人填写】

- 协议布局是否与 Simulink 模型一致：
- 最终周期、优先级是否与当前 DBC 不同：
- 是否需要调整 ID、DLC、周期、字段或枚举：
- 是否完成 `0x305`、`0x502..0x509`、`0x4A4` 的文档：
- 是否同步修改 DBC、接口文档、接收方代码和遥测对照：
- 确认日期和签名：
